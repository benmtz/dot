vim.pack.add({
{
  src = 'https://github.com/edeneast/nightfox.nvim',
  version = 'main'
},
{
  src = 'https://github.com/f-person/auto-dark-mode.nvim',
  version = 'main'
},
})

require("auto-dark-mode").setup({
    set_dark_mode = function()
        vim.api.nvim_set_option_value("background", "dark", {})
        vim.cmd("colorscheme nightfox") -- Set dark colorscheme here
    end,
    set_light_mode = function()
        vim.api.nvim_set_option_value("background", "light", {})
        vim.cmd("colorscheme nightfox")
    end,
    update_interval = 3000,
    fallback = "dark",
})

require("nightfox").setup({
  options = {
    transparent = true,
  },
  specs = {
    nightfox = {
      -- Top-level background/foreground/selection shades
      -- bg0  = "",  -- Dark bg (status line and float)
      -- bg1  = "",  -- Default bg
      -- bg2  = "",  -- Lighter bg (colorcolumn, folds)
      -- bg3  = "",  -- Lighter bg (cursor line)
      -- bg4  = "",  -- Conceal, border fg
      -- fg0  = "",  -- Lighter fg
      -- fg1  = "",  -- Default fg
      -- fg2  = "",  -- Darker fg (status line)
      -- fg3  = "",  -- Darker fg (line numbers, fold columns)
      -- sel0 = "",  -- Popup bg, visual selection bg
      -- sel1 = "",  -- Popup sel bg, search bg

      syntax = {
        -- bracket  = "green",   -- Brackets and punctuation
        -- builtin0 = "green",   -- Builtin variable (e.g. `this`)
        -- builtin1 = "green",   -- Builtin type
        -- builtin2 = "green",   -- Builtin const
        -- builtin3 = "green",   -- (reserved, not used by default)
        -- comment  = "blue",    -- Comments
        conditional = "green.dim",   -- Conditionals and loops
        const       = "green",   -- Constants, imports, booleans
        -- dep      = "green",   -- Deprecated symbols
        field       = "cyan.bright",   -- Object/struct fields
        func        = "green.dim",   -- Functions and titles
        -- ident    = "green",   -- Identifiers
        keyword     = "cyan.bright",   -- Keywords
        number      = "yellow",   -- Numbers
        operator    = "pink.dim",   -- Operators
        preproc     = "yellow.dim",   -- Preprocessor directives
        -- regex    = "green",   -- Regular expressions
        statement   = "white.dim",   -- Statements
        string      = "orange.dim", -- Strings
        -- type     = "blue.dim",   -- Types
        variable    = "green.bright",   -- Variables
      },
      diag = {
        -- error = "",  -- Diagnostic error
        -- warn  = "",  -- Diagnostic warning
        -- info  = "",  -- Diagnostic info
        -- hint  = "",  -- Diagnostic hint
        -- ok    = "",  -- Diagnostic ok
      },

      diag_bg = {
        -- error = "",  -- Diagnostic error background (blended)
        -- warn  = "",  -- Diagnostic warning background (blended)
        -- info  = "",  -- Diagnostic info background (blended)
        -- hint  = "",  -- Diagnostic hint background (blended)
        -- ok    = "",  -- Diagnostic ok background (blended)
      },

      diff = {
        -- add    = "",  -- Diff added line background
        -- delete = "",  -- Diff deleted line background
        -- change = "",  -- Diff changed line background
        -- text   = "",  -- Diff changed text within a line
      },

      git = {
        -- add      = "",  -- Git added
        -- removed  = "",  -- Git removed
        -- changed  = "",  -- Git changed
        -- conflict = "",  -- Git conflict
        -- ignored  = "",  -- Git ignored
      },
    }
  },
  groups = {
    nightfox = {

      -- ── Editor ──────────────────────────────────────────────────────
      -- ColorColumn     = {},  -- columns set with 'colorcolumn'
      -- Conceal         = {},  -- placeholder chars for concealed text
      -- Cursor          = {},  -- character under the cursor
      -- lCursor         = {},  -- cursor under language-mapping
      -- CursorIM        = {},  -- cursor in IME mode
      -- CursorColumn    = {},  -- screen-column at the cursor
      -- CursorLine      = {},  -- screen-line at the cursor
      -- Directory       = {},  -- directory names in listings
      -- DiffAdd         = {},  -- diff: added line
      -- DiffChange      = {},  -- diff: changed line
      -- DiffDelete      = {},  -- diff: deleted line
      -- DiffText        = {},  -- diff: changed text within a changed line
      -- EndOfBuffer     = {},  -- filler lines (~) past end of buffer
      -- ErrorMsg        = {},  -- error messages on the command line
      WinSeparator       = { fg = "palette.green" },  -- column separating vertically split windows
      -- VertSplit       = {},  -- (alias for WinSeparator)
      -- Folded          = {},  -- closed folds
      -- FoldColumn      = {},  -- 'foldcolumn'
      -- SignColumn      = {},  -- column where signs are displayed
      -- Substitute      = {},  -- :substitute replacement text
      -- LineNr          = {},  -- line numbers
      -- CursorLineNr    = {},  -- line number on the cursor line
      -- MatchParen      = {},  -- paired bracket under cursor
      -- ModeMsg         = {},  -- 'showmode' message (e.g. "-- INSERT --")
      -- MoreMsg         = {},  -- |more-prompt|
      -- NonText         = {},  -- chars that don't really exist in the text
      -- Normal          = {},  -- normal text
      -- NormalNC        = {},  -- normal text in non-current windows
      -- NormalFloat     = {},  -- normal text in floating windows
      -- FloatBorder     = {},  -- border of floating windows
      -- Pmenu           = {},  -- popup menu: normal item
      -- PmenuSel        = {},  -- popup menu: selected item
      -- PmenuSbar       = {},  -- popup menu: scrollbar
      -- PmenuThumb      = {},  -- popup menu: scrollbar thumb
      -- Question        = {},  -- hit-enter prompt and yes/no questions
      -- QuickFixLine    = {},  -- current quickfix item
      -- Search          = {},  -- last search pattern highlighting
      -- IncSearch       = {},  -- incremental search highlighting
      -- CurSearch       = {},  -- search result under cursor
      -- SpecialKey      = {},  -- unprintable characters
      -- SpellBad        = {},  -- word not recognised by spellchecker
      -- SpellCap        = {},  -- word that should start with a capital
      -- SpellLocal      = {},  -- word used in another region
      -- SpellRare       = {},  -- word hardly ever used
      -- StatusLine      = {},  -- status line of current window
      -- StatusLineNC    = {},  -- status line of non-current windows
      -- TabLine         = {},  -- tab line: inactive tab label
      -- TabLineFill     = {},  -- tab line: filler
      -- TabLineSel      = {},  -- tab line: active tab label
      -- Title           = {},  -- titles for :set all, :autocmd, etc.
      -- Visual          = {},  -- visual mode selection
      -- VisualNOS       = {},  -- visual mode when not owning the selection
      -- WarningMsg      = {},  -- warning messages
      -- Whitespace      = {},  -- 'listchars' whitespace chars
      -- WildMenu        = {},  -- current match in 'wildmenu' completion
      -- WinBar          = {},  -- window bar of current window
      -- WinBarNC        = {},  -- window bar of non-current windows

      -- ── Syntax ──────────────────────────────────────────────────────
      -- Comment        = {},  -- comments
      -- Constant       = {},  -- constants
      -- String         = {},  -- string literals
      -- Character      = {},  -- character literals
      -- Number         = {},  -- numeric literals
      -- Float          = {},  -- floating-point literals
      -- Boolean        = {},  -- boolean literals
      -- Identifier     = {},  -- variable names
      -- Function       = {},  -- function names
      -- Statement      = {},  -- statements
      -- Conditional    = {},  -- if/then/else/switch
      -- Repeat         = {},  -- for/do/while
      -- Label          = {},  -- case/default
      -- Operator       = {},  -- +, *, sizeof, etc.
      -- Keyword        = {},  -- keywords
      -- Exception      = {},  -- try/catch/throw
      -- PreProc        = {},  -- generic preprocessor
      -- Include        = {},  -- #include
      -- Define         = {},  -- #define
      -- Macro          = {},  -- macros
      -- PreCondit      = {},  -- #if/#else/#endif
      -- Type           = {},  -- int/long/char/etc.
      -- StorageClass   = {},  -- static/register/volatile
      -- Structure      = {},  -- struct/union/enum
      -- Typedef        = {},  -- typedef
      -- Special        = {},  -- special symbols
      -- SpecialChar    = {},  -- special characters in constants
      -- Tag            = {},  -- CTRL-] navigable tags
      -- Delimiter      = {},  -- characters needing attention
      -- SpecialComment = {},  -- special things inside comments
      -- Debug          = {},  -- debugging statements
      -- Underlined     = {},  -- underlined text
      -- Bold           = {},  -- bold text
      -- Italic         = {},  -- italic text
      -- Error          = {},  -- erroneous constructs
      -- Todo           = {},  -- TODO/FIXME/XXX
      -- diffAdded      = {},  -- diff: added lines
      -- diffRemoved    = {},  -- diff: removed lines
      -- diffChanged    = {},  -- diff: changed lines
      -- diffOldFile    = {},  -- diff: old file name
      -- diffNewFile    = {},  -- diff: new file name
      -- diffFile       = {},  -- diff: file header
      -- diffLine       = {},  -- diff: line info (@@ ... @@)
      -- diffIndexLine  = {},  -- diff: index line

      -- ── Treesitter ──────────────────────────────────────────────────
      -- Identifiers
      -- ["@variable"]                      = {},  -- variable names
      -- ["@variable.builtin"]              = {},  -- built-in variables (this, self…)
      -- ["@variable.parameter"]            = {},  -- function parameters
      -- ["@variable.member"]               = {},  -- object/struct fields
      -- ["@constant"]                      = {},  -- constants
      -- ["@constant.builtin"]              = {},  -- built-in constants
      -- ["@constant.macro"]                = {},  -- preprocessor constants
      -- ["@module"]                        = {},  -- modules / namespaces
      -- ["@label"]                         = {},  -- GOTO labels / heredoc labels

      -- Literals
      -- ["@string"]                        = {},  -- string literals
      -- ["@string.regexp"]                 = {},  -- regular expressions
      -- ["@string.escape"]                 = {},  -- escape sequences
      -- ["@string.special"]                = {},  -- other special strings
      -- ["@string.special.url"]            = {},  -- URIs / hyperlinks
      -- ["@character"]                     = {},  -- character literals
      -- ["@character.special"]             = {},  -- special characters / wildcards
      -- ["@boolean"]                       = {},  -- booleans
      -- ["@number"]                        = {},  -- numeric literals
      -- ["@number.float"]                  = {},  -- floating-point literals

      -- Types
      -- ["@type"]                          = {},  -- type / class definitions
      -- ["@type.builtin"]                  = {},  -- built-in types
      -- ["@attribute"]                     = {},  -- attributes / decorators
      -- ["@property"]                      = {},  -- key in key/value pairs

      -- Functions
      -- ["@function"]                      = {},  -- function definitions
      -- ["@function.builtin"]              = {},  -- built-in functions
      -- ["@function.macro"]                = {},  -- preprocessor macros
      -- ["@constructor"]                   = {},  -- constructors
      -- ["@operator"]                      = {},  -- operators

      -- Keywords
      -- ["@keyword"]                       = {},  -- keywords
      -- ["@keyword.function"]              = {},  -- function-defining keywords (def, func)
      -- ["@keyword.operator"]              = {},  -- word operators (and, or)
      -- ["@keyword.import"]                = {},  -- import keywords
      -- ["@keyword.storage"]               = {},  -- storage modifiers (static…)
      -- ["@keyword.repeat"]                = {},  -- loop keywords (for, while)
      -- ["@keyword.return"]                = {},  -- return/yield
      -- ["@keyword.exception"]             = {},  -- exception keywords
      -- ["@keyword.conditional"]           = {},  -- conditional keywords (if, else)
      -- ["@keyword.conditional.ternary"]   = {},  -- ternary operator (? :)

      -- Punctuation
      -- ["@punctuation.delimiter"]         = {},  -- delimiters (; . ,)
      -- ["@punctuation.bracket"]           = {},  -- brackets (( ) { } [ ])
      -- ["@punctuation.special"]           = {},  -- special symbols ({} in interpolation)

      -- Comments
      -- ["@comment"]                       = {},  -- line and block comments
      -- ["@comment.error"]                 = {},  -- ERROR/FIXME/DEPRECATED comments
      -- ["@comment.warning"]               = {},  -- WARNING/HACK comments
      -- ["@comment.todo"]                  = {},  -- TODO/WIP comments
      -- ["@comment.note"]                  = {},  -- NOTE/INFO comments

      -- Markup
      -- ["@markup"]                        = {},  -- general markup text
      ["@markup.strong"] = { fg = "palette.green", style = "bold" }, -- bold text  ← hardcoded orange in nightfox
      ["@text.strong"]   = { fg = "palette.green", style = "bold" }, -- legacy alias for @markup.strong
      ["@markup.italic"]                 = { fg = "palette.green", style = "italic" },  -- italic text
      -- ["@markup.strikethrough"]          = {},  -- struck-through text
      -- ["@markup.underline"]              = {},  -- underlined text
      ["@markup.heading"]                = { fg = "palette.cyan.bright" },  -- headings / titles
      ["@markup.quote"]                  = {},  -- block quotes
      -- ["@markup.math"]                   = {},  -- math environments
      -- ["@markup.link"]                   = {},  -- text references / footnotes
      -- ["@markup.link.label"]             = {},  -- link descriptions
      -- ["@markup.link.url"]               = {},  -- URL-style links
      -- ["@markup.raw"]                    = {},  -- inline code / verbatim text
      -- ["@markup.raw.block"]              = {},  -- fenced code blocks
      ["@markup.list"]                   = { fg = "palette.pink.dim" },  -- list markers
      -- ["@markup.list.checked"]           = {},  -- checked todo markers
      -- ["@markup.list.unchecked"]         = {},  -- unchecked todo markers

      -- Diff (treesitter)
      -- ["@diff.plus"]                     = {},  -- added text in diffs
      -- ["@diff.minus"]                    = {},  -- deleted text in diffs
      -- ["@diff.delta"]                    = {},  -- changed text in diffs

      -- Tags (HTML/XML)
      -- ["@tag"]                           = {},  -- tag names
      -- ["@tag.attribute"]                 = {},  -- tag attributes
      -- ["@tag.delimiter"]                 = {},  -- tag delimiters (< > /)

      -- Language-specific
      -- ["@label.json"]                    = {},  -- JSON keys
      -- ["@constructor.lua"]               = {},  -- Lua tables used as constructors { }
      -- ["@field.rust"]                    = {},  -- Rust struct fields
      -- ["@variable.member.yaml"]          = {},  -- YAML keys

      -- ── Terminal ────────────────────────────────────────────────────
      -- terminal_color_0  = "",  -- black
      -- terminal_color_8  = "",  -- bright black
      -- terminal_color_1  = "",  -- red
      -- terminal_color_9  = "",  -- bright red
      -- terminal_color_2  = "",  -- green
      -- terminal_color_10 = "",  -- bright green
      -- terminal_color_3  = "",  -- yellow
      -- terminal_color_11 = "",  -- bright yellow
      -- terminal_color_4  = "",  -- blue
      -- terminal_color_12 = "",  -- bright blue
      -- terminal_color_5  = "",  -- magenta
      -- terminal_color_13 = "",  -- bright magenta
      -- terminal_color_6  = "",  -- cyan
      -- terminal_color_14 = "",  -- bright cyan
      -- terminal_color_7  = "",  -- white
      -- terminal_color_15 = "",  -- bright white
    }
  },
  palettes = {
    nightfox = {
      green = { base = "{{ glob.green }}", bright="{{ glob.green_intense }}", dim="{{ glob.green_faint }}" },
      red = { base = "{{ glob.red }}", bright="{{ glob.red_intense }}", dim="{{ glob.red_faint }}" },
      yellow = { base = "{{ glob.yellow }}", bright="{{ glob.yellow_intense }}", dim="{{ glob.yellow_faint }}" },
      orange = { base = "{{ glob.orange }}", bright="{{ glob.orange_intense }}", dim="{{ glob.orange_faint }}" },
      magenta = { base = "{{ glob.magenta }}", bright="{{ glob.magenta_intense }}", dim="{{ glob.magenta_faint }}" },
      cyan = { base = "{{ glob.cyan }}", bright="{{ glob.cyan_intense }}", dim="{{ glob.cyan_faint }}" },
      white = { base = "{{ glob.white }}", bright="{{ glob.white_intense }}", dim="{{ glob.white_faint }}" },
      pink = { base = "{{ glob.pink }}", bright="{{ glob.pink_intense }}", dim="{{ glob.pink_faint }}" },
      black = { base = "{{ glob.black }}", bright="{{ glob.black_intense }}", dim="{{ glob.black_faint }}" },
      blue = { base = "{{ glob.blue }}", bright="{{ glob.blue_intense }}", dim="{{ glob.blue_faint }}" },
      bg0 = "{{ glob.black }}",
      bg1 = "{{ glob.black }}",
      bg2 = "{{ glob.black }}",
      bg3 = "{{ glob.black }}",
      bg4 = "{{ glob.black }}",
      fg0 = "{{ glob.white }}",
      fg1 = "{{ glob.white }}",
      fg2 = "{{ glob.white }}",
      fg3 = "{{ glob.white }}",
      fg4 = "{{ glob.white }}",
      sel0 = "{{ glob.cyan }}",
      sel1 = "{{ glob.pink }}",
      comment = "{{ glob.white_faint }}",
    }
  }
})

